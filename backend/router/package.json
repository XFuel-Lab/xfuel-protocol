{
  "name": "xfuel-router",
  "version": "1.0.0",
  "description": "XFUEL Protocol Theta EVM Router",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.js --network thetaTestnet",
    "register:testnet": "hardhat run scripts/register.js --network thetaTestnet",
    "simulate": "node scripts/simulate.js"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.3",
    "ethers": "^6.16.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  }
}

