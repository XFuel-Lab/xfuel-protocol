Open framework adapter doors for Chit402: LangChain + Vercel AI SDK helpers, www docs for Cloudflare/OpenClaw/ACP/Olas-Theoriq, OpenClaw skill, door board updates, and Bazaar docs link hygiene.

Shipped:
- packages/adapters (@xfuel/adapters) with createChitChatOpenAI, createChit, extractReceipt, ingestForeignX402ToBook
- packages/aliases/chit402-adapters public alias
- www routes: /docs/framework-adapters, /docs/cloudflare, /docs/openclaw, /docs/acp, /docs/swarm-platforms (+ /docs/langchain, /docs/ai-sdk, /docs/olas, /docs/theoriq aliases)
- skills/openclaw-chit402/SKILL.md
- docs/doors/later-adapters.md, acp-stamp-path.md, README board
- Eliza plugin page updated to live status; Docs hub + ChitHome integration doors section

Tests: 13 unit tests in packages/adapters (config shape + verify_url extraction from fixture).

npm publish (manual, WebAuthn):
  cd packages/adapters && npm publish --access public
  cd packages/aliases/chit402-adapters && npm publish --access public

Lightsail: docs-only www changes — no deploy required unless www is wired to auto-deploy from main.
